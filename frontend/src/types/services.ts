export interface Service {
    id: number
    name: string
    description: string
    price: string
    estimatedDurationMinutes: number | null
    isActive: number
    createdAt: string
    updatedAt: string   
}
